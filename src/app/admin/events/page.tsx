import { Placeholder } from "@/components/placeholder";

export const metadata = { title: "Events" };

export default function EventsPage() {
  return (
    <Placeholder
      title="Events"
      milestone="M3"
      description="Create and publish tournaments: name, dates, venue, province, and per-athlete fee. Draft events stay hidden."
    />
  );
}