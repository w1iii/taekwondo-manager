import { Placeholder } from "@/components/placeholder";

export const metadata = { title: "Athletes" };

export default function AthletesPage() {
  return (
    <Placeholder
      title="Athletes"
      milestone="M4"
      description="Every registered athlete in one filterable list: chapter, weight class, age group, belt rank."
    />
  );
}