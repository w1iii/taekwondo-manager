import { Placeholder } from "@/components/placeholder";

export const metadata = { title: "Brackets" };

export default function BracketsPage() {
  return (
    <Placeholder
      title="Brackets"
      milestone="M7"
      description="Draw fair random brackets per division, re-shuffle freely, then finalize. Enter live results with auto-advance."
    />
  );
}