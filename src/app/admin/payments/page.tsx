import { Placeholder } from "@/components/placeholder";

export const metadata = { title: "Payments" };

export default function PaymentsPage() {
  return (
    <Placeholder
      title="Payments"
      milestone="M6"
      description="Approvals queue: view each chapter's receipt, then approve or reject with a note so the coach can resubmit."
    />
  );
}