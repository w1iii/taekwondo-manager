import { Placeholder } from "@/components/placeholder";

export const metadata = { title: "Team Payment" };

export default function PaymentsPage() {
  return (
    <Placeholder
      title="Team Payment"
      milestone="M5"
      description="One payment covers the whole chapter roster. Upload proof of payment with a GCash reference number and submit for approval."
    />
  );
}