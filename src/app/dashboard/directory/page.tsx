import { Placeholder } from "@/components/placeholder";

export const metadata = { title: "Directory" };

export default function DirectoryPage() {
  return (
    <Placeholder
      title="Athlete Directory"
      milestone="M9"
      description="Search and browse every registered athlete across all chapters, sorted A to Z with filters by chapter, weight division, age group, and belt rank."
    />
  );
}