import { Placeholder } from "@/components/placeholder";

export const metadata = { title: "Brackets & Schedule" };

export default function BracketsPage() {
  return (
    <Placeholder
      title="Brackets & Schedule"
      milestone="M7"
      description="Live bracket tree and match schedule per division. Updates as organizer enters results; locked until the organizer finalizes."
    />
  );
}